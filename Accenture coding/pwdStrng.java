import java.util.*;
public class pwdStrng {
    public static int isPassWordStrng(String A){
        if(A.length()<4){
            return 0;
        }

        wrong approach--
        for(int i=0;i<A.length();i++){
            if(A.charAt(1)== && )
        }
    }
    public static void main(String[] args) {
        Scanner sc=new Scanner(System.in);
        String A=sc.nextLine();
        sc.close();
        System.out.println(isPassWordStrng(A));
    }
}
